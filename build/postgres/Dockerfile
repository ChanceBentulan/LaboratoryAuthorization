# Use an official PostgreSQL image as a parent image
FROM postgres:latest

# Set environment variables for PostgreSQL (optional, you can customize these)
ENV POSTGRES_USER=admin
ENV POSTGRES_PASSWORD=admin
ENV POSTGRES_DB=prod_latest

# Expose the default PostgreSQL port (5432)
EXPOSE 5432